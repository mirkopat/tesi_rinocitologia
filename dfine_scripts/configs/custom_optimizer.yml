epochs: 50
clip_max_norm: 0.1

optimizer:
  type: AdamW
  params:
    - params: '^(?=.*backbone).*$'
      lr: 0.00001
    - params: '(?!.*backbone).*$'
      lr: 0.0001
  lr: 0.0001
  betas: [0.9, 0.999]
  weight_decay: 0.0001

lr_scheduler:
  type: MultiStepLR
  milestones: [40, 45]
  gamma: 0.1